# Basic Server Settings
Port 4242                          # Non-standard port
AddressFamily inet                 # IPv4 only
ListenAddress 0.0.0.0              # Listen on all interfaces

# Authentication
PermitRootLogin no                 # Forbid root login
MaxAuthTries 3                     # Max login attempts
PasswordAuthentication no          # Disable password auth
PubkeyAuthentication yes           # Allow only SSH keys
AuthenticationMethods publickey    # Only key-based auth
PermitEmptyPasswords no            # No empty passwords

# Security Settings
Protocol 2                         # Only SSH Protocol 2
LoginGraceTime 30                  # Login timeout
X11Forwarding no                   # X11 forwarding off
AllowTcpForwarding no              # TCP forwarding off
AllowAgentForwarding no            # Agent forwarding off
PermitTunnel no                    # Tunneling off

# Connection Control
MaxStartups 10:30:100              # Connection limits
MaxSessions 2                      # Max sessions per connection
ClientAliveInterval 300            # Keepalive every 5 min
ClientAliveCountMax 3              # Max 3 missed keepalives

# Logging
LogLevel VERBOSE                   # Detailed logging
SyslogFacility AUTH                # Syslog category

# Access Control
AllowUsers your_username           # Allow only specific users
DenyUsers root                     # Explicitly deny root

# Encryption
Ciphers chacha20-poly1305@openssh.com,aes256-gcm@openssh.com
MACs hmac-sha2-512-etm@openssh.com,hmac-sha2-256-etm@openssh.com
KexAlgorithms curve25519-sha256@libssh.org,diffie-hellman-group16-sha512